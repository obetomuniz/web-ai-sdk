# .agents

Workspace-level AI agent configuration.

- `agents.md` is the canonical project instruction file, following the
  `.agents` protocol layout.
- The root `AGENTS.md` is kept as a compatibility symlink for tools that still
  discover only the older AGENTS.md convention.

Do not put secrets, tokens, or machine-local MCP credentials in this directory.
