node_modules/
dist/
.turbo/
coverage/
storybook-static/
_site/
.astro/
*.log
.DS_Store
.env
.env.local

# Sub-package / sub-app npmrc files (the root .npmrc is intentionally tracked for pnpm config; never commit auth tokens).
packages/*/.npmrc
apps/*/.npmrc
.vscode/
.idea/
# Claude Code session artifacts (screenshots, launch config) but keep skills.
.claude/*.jpg
.claude/*.jpeg
.claude/*.png
.claude/launch.json
