---
"@web-ai-sdk/webmcp": patch
---

Add the optional `debugging` tool annotation. Preserve explicit values through registration and discovery. Document that the field categorizes debugging and developer-tooling tools and does not authorize execution.
